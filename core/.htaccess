# Block all direct browser access to this folder.
# These are class files, only ever loaded via require/include.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
